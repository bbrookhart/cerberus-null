schema_version: "1.0"
mission:
  id: MISSION-001
  objective: Investigate synthetic suspicious authentication activity.
  agent: agent-investigator-01
  controller: human-operator-01
  allowed_resources:
    - siem-*
    - endpoint-*
    - identity-*
    - ticket-*
  maximum_action_tier: T1
  autonomy_level: A2
  validity: runtime_fixture
note: The executable TEVV fixture widens resource visibility and tier ceiling per isolated test.
