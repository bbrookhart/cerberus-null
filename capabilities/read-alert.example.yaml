schema_version: "1.0"
capability:
  issuer: human-operator-01
  subject: agent-investigator-01
  action: read_alert
  resources: [siem-lab-*]
  mission: MISSION-001
  valid_for_seconds: 300
  max_uses: 1
  requires_approval: false
  delegable: false
note: Example input only. Runtime artifacts are signed; this file grants no authority.
